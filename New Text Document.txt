int maxResponseTime = 50; // ms
double avgResponseTime = ctx.getPreviousSampler().getTime();

if(avgResponseTime > maxResponseTime) {
    Failure = true;
    FailureMessage = "Tempo de resposta " + avgResponseTime + "ms excedeu 50ms";
}
