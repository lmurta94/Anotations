import org.apache.jmeter.protocol.http.sampler.HTTPSampleResult;

// Obter o tempo de resposta médio do último resultado
float responseTime = prev.getEndTime() - prev.getStartTime();

// Validar se tempo é superior a 50ms
if (responseTime > 50) {
    Failure = true;
    FailureMessage = "Tempo médio de resposta (" + responseTime + "ms) excedeu 50ms";
}
